#!/usr/bin/env python3
'''A function which takes a list of floats as
argument and returns their sum as a float'''
from typing import List


def sum_list(input_list: List[float]) -> float:
    '''A function which takes a list of floats as
argument and returns their sum as a float'''
    return float(sum(input_list))
